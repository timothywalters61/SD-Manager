version: 2
jobs:
    build:
        docker:
            - image: circleci/node:10.16.0
        working_directory: ~/repo
        steps:
            - run:
                  name: checking if everything is fine with you guys
                  command: npm install