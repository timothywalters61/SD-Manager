version: 2
jobs:
    build:
        docker:
            - image: circleci/node:10.16.0
        working_directory: ~/repo
        steps:
            - run:
                  name: installing npm dependencies
                  command: npm install
            - run:
                  name: installing npm & puppeteer
                  command: cd public/public/tests
            # - run:
            #       name: run tests
            #       command: cd public/ ; cd public/ ; cd tests/ ; npm run test