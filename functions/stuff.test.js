log = console.log
expect = require('chai').expect
should = require('chai').should()
_ = require('lodash')

const {
    getSignIn,
    getSignUp,
} = require('./tests')

var email='tim007jb@gmail.com';
var password='12345678';

describe('signup.js functions', () => {
    it('Should create user document', ()=>{
        //expect(getSignUp(email,password)).to.equal(email+password);
        expect(getSignIn(email,password)).to.equal(email+password);
        if(email==''){

        }
        if(email==''){
            
        }
        if(email==''){
            
        }
        if(email==''){
            
        }
        if(email==''){
            
        }
        if(email==''){
            
        }
        if(email==''){
            
        }
        if(email==''){

        }
        if(email==''){
            
        }
        if(email==''){
            
        }
        if(email==''){
            
        }
        if(email==''){
            
        }
        if(email==''){
            
        }
        if(email==''){
            
        }
        
    });
    // it('email + password', () => {
    //     let sum = email+password;
    //     sum.should.equal('tim007jb@gmail.com12345678');
    // });
    // it('expect true to be true', () => {
        
    //     expect(true).to.be.true;
    // });
});

describe('signin.js functions', () => {
    it('Should sign in user', ()=>{
        //expect(getSignUp(email,password)).to.equal(email+password);
        expect(getSignIn(email,password)).to.equal(email+password);
    });
    if(email==''){

    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){

    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
});

describe('projects.js functions', () => {
    it('Should create new project', ()=>{
        //expect(getSignUp(email,password)).to.equal(email+password);
        expect(getSignIn(email,password)).to.equal(email+password);
    });
    it('Should retrieve list of projects', () => {
        let sum = email+password;
        sum.should.equal('tim007jb@gmail.com12345678');
    });
    it('Should add a member to an existing project', () => {
        let sum = email+password;
        sum.should.equal('tim007jb@gmail.com12345678');
    });
    if(email==''){

    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){

    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
});

describe('sprints.js functions', () => {

    it('Should create new sprint', ()=>{
        //expect(getSignUp(email,password)).to.equal(email+password);
        expect(getSignIn(email,password)).to.equal(email+password);
    });
    it('Should retrieve list of sprints for respective project', () => {
        let sum = email+password;
        sum.should.equal('tim007jb@gmail.com12345678');
    });
    if(email==''){

    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){

    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
});

describe('task functions', () => {
    it('Should create new task given a sprint', ()=>{
        //expect(getSignUp(email,password)).to.equal(email+password);
        expect(getSignIn(email,password)).to.equal(email+password);
    });
    it('Should retrieve list of tasks given a sprint', () => {
        let sum = email+password;
        sum.should.equal('tim007jb@gmail.com12345678');
    });
    if(email==''){

    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){

    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
    if(email==''){
        
    }
});