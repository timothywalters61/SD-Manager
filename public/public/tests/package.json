{
  "name": "tests",
  "version": "1.0.0",
  "description": "tests",
  "main": "tests.js",
  "dependencies": {
    "jest": "^26.2.2",
    "puppeteer": "^5.2.1"
  },
  "devDependencies": {
    "coveralls": "^3.1.0"
  },
  "scripts": {
    "test": "jest --detectOpenHandles",
    "cover": "jest --detectOpenHandles --coverage && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage || true",
    "move": "mv ./coverage/ ../../../"
  },
  "author": "Timothy Walters",
  "license": "ISC"
}
